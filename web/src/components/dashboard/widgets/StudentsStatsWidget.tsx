import { WidgetPlaceholder } from "./_shared";

export function StudentsStatsWidget() {
  return <WidgetPlaceholder title="StudentsStats" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
