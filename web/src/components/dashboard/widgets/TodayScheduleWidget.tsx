import { WidgetPlaceholder } from "./_shared";

export function TodayScheduleWidget() {
  return <WidgetPlaceholder title="TodaySchedule" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
