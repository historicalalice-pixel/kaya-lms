import { WidgetPlaceholder } from "./_shared";

export function PendingReviewsWidget() {
  return <WidgetPlaceholder title="PendingReviews" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
