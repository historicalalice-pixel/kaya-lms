import { WidgetPlaceholder } from "./_shared";

export function QuickActionsWidget() {
  return <WidgetPlaceholder title="QuickActions" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
