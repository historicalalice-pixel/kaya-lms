import { WidgetPlaceholder } from "./_shared";

export function TestsWidget() {
  return <WidgetPlaceholder title="Tests" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
