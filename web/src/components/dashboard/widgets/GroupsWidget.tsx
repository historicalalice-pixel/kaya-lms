import { WidgetPlaceholder } from "./_shared";

export function GroupsWidget() {
  return <WidgetPlaceholder title="Groups" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
