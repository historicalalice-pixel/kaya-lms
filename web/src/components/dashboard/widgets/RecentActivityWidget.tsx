import { WidgetPlaceholder } from "./_shared";

export function RecentActivityWidget() {
  return <WidgetPlaceholder title="RecentActivity" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
