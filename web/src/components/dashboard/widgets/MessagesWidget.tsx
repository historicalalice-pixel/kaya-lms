import { WidgetPlaceholder } from "./_shared";

export function MessagesWidget() {
  return <WidgetPlaceholder title="Messages" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
