import { WidgetPlaceholder } from "./_shared";

export function CoursesWidget() {
  return <WidgetPlaceholder title="Courses" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
