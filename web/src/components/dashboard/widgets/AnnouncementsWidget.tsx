import { WidgetPlaceholder } from "./_shared";

export function AnnouncementsWidget() {
  return <WidgetPlaceholder title="Announcements" lines={["Дані будуть тут", "Стан синхронізації: активний", "Останнє оновлення: щойно"]} />;
}
